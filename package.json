{
  "name": "prompts",
  "version": "0.0.0",
  "private": true,
  "description": "Utilities and prompts workspace",
  "license": "MIT",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
